Header theme cycler — one click moves 시스템 → 라이트 → 다크, toggling `.dark` on `<html>` and persisting to localStorage. Icon is monitor / sun / moon. In system mode it live-follows the OS.

```jsx
<ThemeToggle />
```
